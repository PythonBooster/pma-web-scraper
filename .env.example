# =============================================
# Конфигурация для доступа к phpMyAdmin (PHPMyAdmin Configuration)
# =============================================

# Базовый URL phpMyAdmin (без конкретной страницы)
# Base URL of phpMyAdmin (without specific page)
# Пример/Example: http://localhost/phpmyadmin/ или/or http://server-ip/phpmyadmin/
PMA_BASE_URL=http://your-server-address/phpmyadmin/

# Имя пользователя для авторизации в phpMyAdmin
# Username for phpMyAdmin authentication
PMA_USER_NAME=your_username

# Пароль для авторизации в phpMyAdmin
# Password for phpMyAdmin authentication
PMA_PASSWORD="your_strong_password*"

# Название базы данных для подключения
# Database name to connect to
PMA_DB_NAME=your_database_name

# Название таблицы для работы
# Table name to interact with
PMA_TABLE_NAME=your_table_name

# =============================================
# Важные примечания (Important Notes):
# 1. Никогда не коммитьте реальный .env в Git!
#    Never commit real .env to Git!
# 2. Для спецсимволов в пароле используйте кавычки
#    For special characters in password use quotes
# 3. Убедитесь что URL заканчивается на /
#    Ensure URL ends with /
# =============================================
